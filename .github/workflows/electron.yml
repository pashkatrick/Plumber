name: Build
on: [push, pull_request]

jobs:
  # build_on_mac:
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-node@master
  #       with:
  #         node-version: 10
  #     - name: Step 1 - Install dependencies
  #       run: |
  #         npm install
  #         npm run npm-install
  #     - name: Step 2 - Build packages and binaries
  #       run: |
  #         curl -L https://github.com/fullstorydev/grpcurl/releases/download/v1.8.1/grpcurl_1.8.1_osx_x86_64.tar.gz -o grpc.tar.gz
  #         tar -zxvf grpc.tar.gz
  #         mv grpcurl ./node_modules/electron/dist/Electron.app/Contents/Resources/grpcurl
  #         rm -rf grpc.tar.gz
  #         npm run mac-pkg
  #         npm run dmg
  #         ls -la release-build
  #         ls -la release-build-dmg
  #     - name: Step 3 - Use the Upload Artifact GitHub Action
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: plumber.dmg
  #         path: release-build-dmg/Plumber.dmg

  build_on_win:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@master
        with:
          node-version: 10
      - name: Step 1 - Install dependencies
        run: |
          npm install
          npm run npm-install
      - name: Step 2 - Build packages and binaries
        run: |
          curl -L https://github.com/fullstorydev/grpcurl/releases/download/v1.8.1/grpcurl_1.8.1_windows_x86_64.zip -o grpc_64.tar.gz
          tar -zxvf grpc_64.tar.gz
          ls .
          mv grpcurl.exe .\node_modules\electron\dist\resources\grpcurl
          ls .
          del /а ./grpc_64.tar.gz
          npm run win-pkg
          npm run exe        
          ls release-build
          ls release-build-exe
      - name: Step 3 - Use the Upload Artifact GitHub Action
        uses: actions/upload-artifact@v2
        with:
          name: plumber.exe
          path: release-build-exe/*.exe
